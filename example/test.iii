interaction
  (main):Number out
with
    interaction
      (b):Number out
    is
      ((#2) in (js`o=5;`)(#1)=(#2))
    interaction
      ((x:Number in)+(y:Number in)):Number out
    is
      ((#3) in (js`o=i.x+i.y;`) ({x:(x),y:(y)}) = (#3))
is
  ((b)+(b))
